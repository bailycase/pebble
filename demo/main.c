#include "main.h"
#include <string.h>

int main()
{
    init_pb();
    // char** a;
    // char**b;
    // a = malloc(sizeof(char*));
    // b = malloc(sizeof(char*) + 1);

    // a[0] = "test";
    // a[1] = "test2";

    // for (int i = 0; i < 2; i++)
    // {
    //     b[i] = a[i];
    // }

    // size_t c = sizeof(b);

    // b[2] = "test3";

    // printf("%s\n", b[0]);
    // printf("%s\n", b[1]);
    // printf("%s\n", b[2]);
    return 0;
}